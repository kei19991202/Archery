<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="android.kei1999.archery">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="Archer&apos;s Note"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".TodaysMenu" />
        <activity android:name=".TheTotalNumberOfMorphisms" />
        <activity android:name=".tuning"></activity>
        <activity android:name=".PointActivity"></activity>
    </application>

</manifest>